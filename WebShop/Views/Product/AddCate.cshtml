@model WebShop.Model.ProductCate
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>AddCate</title>
</head>
<body>
    <div>
        @using (Html.BeginForm())
        {
            <label>分类名称</label> @Html.TextBoxFor(a => a.CateName)
            <label>父级分类</label> @Html.DropDownList("pid",ViewBag.list as List<SelectListItem>)
            <input type="submit" value="添加" />
        }

    </div>
</body>
</html>
